.DS_Store
# secrets / local env — NEVER commit (repo is public)
.env
.env.local
credentials.json
token.json
!.env.example
bin/
obj/
publish/
artifact/
*.zip
*.user
.vs/

# Logo R&D — only the chosen, exported final art ships in imagery/
imagery/concept/
